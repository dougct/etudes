mod balance_factors;
mod evaluate_polynomials;
mod generate_binary_nums;
mod generate_permutations;
mod maximum_consecutive_subsequence;

fn main() {
    // This is a minimal main function for the binary crate
    // The actual functionality is in individual modules with their own tests
    println!("Run tests with: cargo test");
}
